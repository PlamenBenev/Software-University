function asd(first,second,thirth){
    console.log(first.length + second.length + thirth.length);
    let avrg = (first.length + second.length + thirth.length) / 3
    console.log(Math.floor(avrg));
}
asd('chocolate', 'ice cream', 'cake');
